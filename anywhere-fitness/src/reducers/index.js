//import {} from "../actions/index"

const initialState = {
  classList: [],
  isLoading: false,
  error: ''
}

const reducer = ( state = initialState, action) => {
  switch(action.type){
    default:
      return state;
  }
}

export default reducer;